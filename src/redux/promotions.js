import { PROMOTIONS } from '../shared/promotion';
import * as ActionTypes from './ActionTypes';

export const promotion = (state = PROMOTIONS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};